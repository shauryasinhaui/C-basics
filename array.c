#include<stdio.h>
int main() {
    //creation of an array
    int arr[10], n
    printf("Enter the number of element");
    scanf("%d",&arr);
    printf("Enter the elements in an array");
    for(int i=0; i<n; i++) {
        scanf("%d",&arr[i]);
        printf("")
    //insertion of an array
    printf("insertion of an element in an array") {
        if(n-1 == 0) {
            printf("fvubbih")
        }
    }



    }

}